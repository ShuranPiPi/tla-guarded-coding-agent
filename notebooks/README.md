# Notebooks

Maintained demos:

- `01_tlc_spec_guarded_agent_demo.ipynb`: runs the spec-first agent on a sample task.
- `02_provider_fallback_demo.ipynb`: inspects OpenAI/Gemini provider selection.

Historical drafts live in `archive/`. They are preserved for reference only and
are not expected to match the current package API.
